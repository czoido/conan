@Library('conan_ci@use_pytest') _

properties([
    parameters([
        string(name: "test_module", defaultValue: "\"conans/test\""),
        string(name: "node_name", defaultValue: "")
    ]),
]) 

conanCI.runBuild(this, params.test_module, params.node_name)
